<div class="container">
  <table class="table table-bordered">
    <tr>
      <th>Title</th>
      <td><%= @todo.title %></td>
    </tr>
    <tr>
      <th>Due Date</th>
      <td><%= @todo.due_date %></td>
    </tr>
    <tr>
      <th>Note</th>
      <td><%= @todo.note %></td>
    </tr>
  </table>

  <%= link_to 'Back to index', todos_path %>
</div>